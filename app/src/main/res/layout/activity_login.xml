<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LoginActivity"
    android:background="@color/color_background">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="LoginScreen"
        android:id="@+id/textView"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textColor="#ffffff" />

    <EditText
        android:id="@+id/id_campoEmail"
        android:layout_width="@dimen/width_fields"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="false"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:background="#ffffff"
        android:ems="10"
        android:hint="Email"
        android:inputType="textEmailAddress"
        android:padding="@dimen/padding_fields" />

    <EditText
        android:layout_width="@dimen/width_fields"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/id_campoSenha"
        android:layout_marginTop="15dp"
        android:padding="@dimen/padding_fields"
        android:background="#ffffff"
        android:hint="Senha"
        android:layout_below="@+id/id_campoEmail"
        android:layout_alignStart="@+id/id_campoEmail" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Criar Conta"
        android:id="@+id/id_botaoCriarConta"
        android:layout_alignBottom="@+id/id_botaoEntrar"
        android:layout_toStartOf="@+id/id_botaoEntrar"
        android:textColor="#000000" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Entrar"
        android:id="@+id/id_botaoEntrar"
        android:layout_below="@+id/id_campoSenha"
        android:layout_alignEnd="@+id/id_campoSenha"
        android:layout_marginTop="30dp"
        android:textColor="#000000" />
</RelativeLayout>
